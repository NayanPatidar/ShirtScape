import { createContext, useEffect, useState } from "react";

export const CartContext = createContext();
export const SearchContext = createContext();
export const AddressUpdateContext = createContext();
